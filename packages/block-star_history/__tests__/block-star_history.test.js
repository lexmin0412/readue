'use strict';

const blockStarHistory = require('..');
const assert = require('assert').strict;

assert.strictEqual(blockStarHistory(), 'Hello from blockStarHistory');
console.info('blockStarHistory tests passed');
